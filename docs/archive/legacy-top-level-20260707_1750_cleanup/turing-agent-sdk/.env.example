# turing-agent-sdk 默认启动 PATH 中的 turing。
# 如果你没有全局 turing，或者要指定本地构建产物，设置这个变量。
# 先运行 `command -v turing` 查看绝对路径，再填到下面。
TURING_BINARY_PATH=

# turing-agent-sdk 走的是本机 Turing Code runtime。
# key 通常由 turing / provider wrapper / ~/.claude/settings.json 管理；不要把真实 key 提交到仓库。
