# Python demo

安装：

```bash
pip install turing-agent-sdk
```

运行：

```bash
python3 python/01-minimal-query.py
```

如果没有全局 `turing`，先指定：

```bash
TURING_BINARY_PATH="$(command -v turing)" python3 python/01-minimal-query.py
```
